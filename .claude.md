# Kmap 项目上下文缓存（Claude）

> 更新时间：`2026-03-12`
>
> 作用：这份文件是仓库级“上下文缓存”。阅读源码、对外说明项目状态、或准备继续开发时，优先参考这里；任何功能、接口、目录结构或完成度判断发生变化后，先更新本文件，再更新 `README.md`。

## 1. 项目定位

- 项目名称：`Kmap`
- 核心定位：**纯本地优先+云端可选**的知识管理/知识图谱应用，实现「安装即离线可用，按需切换云端」的核心目标
- 目标形态：
  - 第一优先级：Android 端纯本地离线应用（当前核心开发方向）
  - 第二优先级：Windows 桌面端应用 + 内网穿透服务端能力
  - 第三优先级：云端部署形态（原有能力100%保留）
- 前端界面层：`React 19 + TypeScript + Vite 6 + Tailwind CSS 4`
- Android 宿主：`Capacitor Android 8.x`
- 后端：`Python 3.11 + FastAPI + SQLAlchemy`
- 应用 ID：`com.karovia.kmap`
- 协作语言：统一使用中文
- 浏览器形态只用于开发调试，不作为独立 Web 产品交付

## 2. 当前工作树的真实状态

### 2.1 已落地能力

**应用壳层与导航**
- `src/App.tsx` 使用 `React Router + HashRouter` 管理一级页面路由
- 已有底部导航和页面切换动画，页面包括首页、文档、图谱、聊天、设置、配置指南

**文档管理**
- `DocumentsPage` 已接入文档列表、搜索、状态筛选、删除、上传进度和状态轮询
- 浏览器上传已可用
- Android 代码链路已实现：文件选择 → `Filesystem.readFile()` 读取 base64 → 转回 `File` → 调用上传接口
- 后端已提供 `/api/v1/documents/upload`、列表、详情、删除、状态查询

**服务商配置**
- 设置页已支持区分 `chat` / `embedding` 两种用途
- 前端支持服务商列表、创建、编辑、删除、测试连接
- 后端已提供 `/api/v1/providers` CRUD 与 `/api/v1/providers/{id}/test`
- 配置指南页已补充火山方舟对话模型 / Embedding 示例

**聊天**
- `ChatPage` 已调用 `/api/v1/chat`
- 支持基于当前消息列表发起一次非流式聊天请求
- 响应中会显示实际命中的默认对话服务商名称

**图谱**
- `GraphPage` 已调用 `/api/v1/graph/overview`
- 当前图谱页展示的是“文档状态 + 服务商 + 文档节点”的概览视图
- 已支持节点选中、摘要信息和节点元数据面板

**平台桥接**
- `src/platform/` 已统一封装平台识别、文件选择、文件读取、权限申请/检查、分享、保存文件、设备信息、返回键监听
- 页面层没有直接散落调用 Capacitor API，平台抽象基本成立
- API 访问在 Android / Capacitor 环境中不能依赖 Vite 代理，前端需要通过 `VITE_API_BASE_URL` 指向真实可达的后端地址
- 本地开发如果 `8000` 端口被其他进程占用，应同步调整后端端口与 `VITE_API_PROXY_TARGET`

**后端基础设施**
- `backend/app/` 已具备模型、Schema、CRUD、API 路由和服务层
- 健康检查接口已提供 `/health` 与 `/health/detailed`
- 文档处理服务已实现：上传落盘、RabbitMQ 投递、Unstructured 分片、Embedding、PostgreSQL / Qdrant 写入

### 2.2 仍属半成品或需要谨慎表述的部分

**首页**
- `DashboardPage` 仍是静态展示和 mock 数据，不应描述为真实业务仪表盘

**图谱**
- 当前 `/api/v1/graph/overview` 不是实体关系图谱，只是概览型聚合数据
- 还没有真实的 Nebula Graph 查询、路径分析、搜索、缩放/平移等深度图谱能力

**聊天**
- 当前是基础非流式问答
- 还没有历史会话持久化、RAG 检索增强、引用来源、流式返回

**文档处理**
- 代码链路已存在，但完整效果依赖 PostgreSQL、RabbitMQ、Qdrant、Unstructured 与可用 Embedding 服务商
- 删除文档时已增加 `Qdrant` 关联向量清理逻辑，仍需联调验证删除结果与异常处理

**服务商支持边界**
- Schema 层允许 `gemini/openai/claude/custom`
- 后端 `LLMFactory` / `EmbeddingFactory` 已接入 Gemini 对话与 Embedding 代码路径
- OpenAI、Claude、自定义 OpenAI 兼容服务仍是当前主要联调主线；Gemini 应视为“代码已接入，待真实服务商验证”
- 本地开发环境如果未安装 `google-generativeai`，后端仍可启动，但 Gemini provider 会在实际调用时报出明确提示

**Android 真机**
- Capacitor 工程和平台接口都已存在
- 但权限、分享、返回键、安全区、原生文件路径读取等仍需真机验证，不能把“代码已写”描述成“已验收”
- Android release 签名依赖 `android/keystore.properties` 与本地 keystore；`app/build.gradle` 已按 Android 根目录解析签名文件路径

## 3. 目录与模块认知

```text
src/        前端页面、组件、平台桥接、状态管理、服务层
android/    Capacitor Android 宿主工程
backend/    FastAPI 后端、文档处理、LLM 服务商、图谱概览接口
docs/       PRD、方案、评审、计划文档（部分为历史设计稿）
scripts/    构建与辅助脚本
```

**前端关键目录**
- `src/pages/`：页面入口
- `src/components/`：页面级复用 UI
- `src/hooks/`：页面状态编排，如 `useDocuments`
- `src/platform/`：统一平台能力出口
- `src/services/`：API / HTTP / 领域服务封装
- `src/stores/`：Zustand 状态
- `src/types/`：领域类型定义

**后端关键目录**
- `backend/app/api/`：FastAPI 路由
- `backend/app/core/`：配置与 LLM 接入
- `backend/app/services/`：文档处理等业务服务
- `backend/app/db/`：数据库与中间件连接
- `backend/app/models/` / `schemas/` / `crud/`：数据模型与访问层
- `backend/app/worker/`：RabbitMQ 文档处理消费者

## 4. 关键接口一览

- `GET /health`：基础健康检查
- `GET /health/detailed`：依赖服务健康检查
- `POST /api/v1/documents/upload`：上传文档
- `GET /api/v1/documents`：文档列表
- `GET /api/v1/documents/{id}`：文档详情
- `GET /api/v1/documents/{id}/status`：文档状态
- `DELETE /api/v1/documents/{id}`：删除文档
- `GET /api/v1/providers`：服务商列表
- `POST /api/v1/providers`：创建服务商
- `PUT /api/v1/providers/{id}`：更新服务商
- `DELETE /api/v1/providers/{id}`：删除服务商
- `POST /api/v1/providers/{id}/test`：测试服务商
- `POST /api/v1/chat`：基础聊天接口
- `GET /api/v1/graph/overview`：图谱概览接口

## 5. 已读重要文档摘要

**产品与规划**
- `docs/产品需求文档.md`：定义了 Android 本地知识图谱产品目标、核心能力与 MVP 范围
- `docs/项目开发进度与计划.md`：早期推进计划，保留了分层思路，但其中“未接路由 / 未初始化 Capacitor”等描述已经过时
- `docs/安卓本地应用开发任务清单.md`：适合用来追踪 Android 真机与桥接类待办

**前端与 Android**
- `docs/前端代码分析报告.md`：解释了页面拆分、平台抽象和交互结构
- `docs/Capacitor Android 接入方案.md`：梳理了从 Web 调试转向 Android 宿主的接入思路

**后端**
- `backend/README.md`：后端技术栈、依赖服务与启动方式
- `backend/QUICKSTART_LLM.md`：LLM 服务商模块的快速启动说明
- `backend/docs/LLM_PROVIDER.md`：服务商配置模型、接口与使用方式
- `backend/docs/document_module.md`：文档处理链路的目标设计与运行说明
- `docs/后端技术栈初步方案.md` / `docs/后端技术栈选型报告.md`：记录后端架构选型过程

**项目管理**
- `docs/GitHub-Issue-推进计划.md`：当前 GitHub Issue 拆解与推进计划
- `docs/Kmap移动端主攻方向开发计划.md`：围绕 Android MVP、体验优化、本地模型落地的阶段性推进计划
- `docs/基于Kmap项目的「纯本地优先+云端可选」完整重构方案.md`：当前核心开发 roadmap，纯本地化架构重构的完整实施计划
- `docs/Kmap本地模型支持技术方案.md`：本地模型选型、云/本地兼容架构与端侧推理方案草案
- `docs/移动端联调问题排查说明.md`：记录 2026-03-12 Android 真机联调时的 API 地址、端口占用和后端依赖问题
- `Kmap-P1-Preview开发进度计划表.md`：P1 Preview 阶段的推进与排期记录
- `scripts/start-frontend-dev.cmd` / `scripts/start-backend-dev.cmd`：当前 Windows 本地联调用的一键启动脚本
- `scripts/start-dev-services.cmd`：同时启动前后端的总入口脚本

## 6. 协作判断口径

- 如果历史文档与源码冲突，以当前工作树源码为准，其次以本文件和 `README.md` 为准
- “页面存在”不等于“业务已完成”
- “接口存在”不等于“链路已真机 / 生产环境验证”
- 对完成度统一使用：`已落地`、`部分完成`、`概览版 / 原型`、`规划中`
- 涉及接口、能力边界或项目状态变化时，必须同步更新 `.claude.md` 和 `README.md`

## 7. 当前优先事项（最高优先级：纯本地化重构）

### 第一阶段：Android 纯本地优先架构重构（当前最高优先级）
- 阶段1：基础架构搭建与双模式网关适配（1-2天）
  - 新增`src/services/gateway.ts`统一网关，封装所有后端API
  - 改造前端所有页面，将直接调用axios的逻辑替换为网关层调用
  - 在设置页新增「纯本地模式 / 云端模式」切换开关
  - 验证云端模式下所有原有功能完全正常
- 阶段2：本地存储与文档管理本地化落地（3-4天）
  - Android工程集成Room数据库、sqlite-vss向量扩展
  - 集成Android本地文档解析库（PdfBox-Android + POI-Android）
  - 开发Capacitor自定义插件，封装文档管理全量接口，与原后端规范对齐
  - 验证纯本地模式下文档上传、解析、列表、删除功能完全离线可用
- 阶段3：本地AI引擎与服务商配置本地化落地（3-4天）
  - Android工程集成ONNX Runtime Mobile，适配量化的Embedding/LLM模型
  - 封装本地模型推理接口，与云端服务商接口规范对齐
  - 扩展服务商配置模块，新增「本地模型」服务商类型
  - 验证纯本地模式下模型配置、本地问答、Embedding生成完全离线可用
- 阶段4：RAG聊天与图谱能力本地化落地（2-3天）
  - 实现完整的本地RAG问答链路，补齐流式响应、会话历史、引用来源能力
  - 实现本地实体关系提取、图谱存储与查询接口
  - 验证纯本地模式下带RAG的聊天、图谱展示功能完全正常
- 阶段5：全链路适配、优化与打包（2-3天）
  - 完成本地仪表盘实时数据统计
  - 全链路双模式测试，确保切换无缝
  - APK体积优化，支持模型按需内置/下载
  - 最终验证：打包后的APK安装后纯离线可用，同时可一键切换云端模式

### 第二阶段：Windows 端移植（纯本地化完成后）
- 基于Electron或Tauri移植前端与本地能力到Windows桌面端
- 集成Windows本地存储、向量引擎、AI推理能力
- 实现与Android端一致的纯本地运行能力

### 第三阶段：内网穿透与跨端服务（Windows移植完成后）
- 实现Windows端作为本地服务端能力
- 集成内网穿透功能，支持同一局域网内Android端访问Windows端服务
- 云端API调用模型能力作为纯本地服务的一部分，支持多端共享
