# Kmap 项目 Claude 协作配置

## 1. 项目定位

- 项目名称：Kmap
- 交付形态：Android 本地应用
- 前端界面层：`React 19 + TypeScript + Vite 6 + Tailwind CSS 4`
- Android 宿主：`Capacitor Android 8.x`
- 后端：`Python 3.11 + FastAPI`
- 应用 ID：`com.karovia.kmap`
- 文档、注释、沟通语言：统一使用中文
- 浏览器运行仅用于开发调试，不作为独立 Web 产品交付

## 2. 截止 2026-03-11 的实际完成状态

### 2.1 已经落地的部分

**前端架构**
- `src/App.tsx` 已收敛为应用壳层，使用 `React Router + HashRouter`
- 路由配置完整，支持页面切换动画（基于 `motion`）
- 核心一级页面已拆分到 `src/pages/`：Dashboard、Documents、Graph、Chat、Settings、ProviderGuide
- 底部导航组件已独立到 `src/components/navigation/BottomNav.tsx`

**文档管理模块**
- `DocumentsPage` 已拆分为页面、Hook（`useDocuments`）与多个文档组件
- 支持文档上传、列表展示、搜索、状态筛选、删除、状态轮询
- 前后端已打通基础链路：`/api/v1/documents`

**服务商配置模块**
- `SettingsPage` 已拆分为页面、服务商列表、表单弹窗、状态提示、配置指南
- 支持服务商列表、创建、编辑、删除、测试连接
- 前后端已打通基础链路：`/api/v1/providers`

**基础设施层**
- `src/services/`：HTTP 客户端、文档服务、服务商服务已建立
- `src/platform/`：平台能力统一接口已封装（文件选择、权限、分享、保存、设备信息、返回键）
- `src/stores/`：Zustand store 已建立（`providerStore`、`platformStore`）
- `src/hooks/`：自定义 Hook 已建立（`useDocuments`）
- `src/types/`：类型定义已建立（`domain.ts`、`navigation.ts`、`platform/types.ts`）
- `src/utils/`：工具函数已建立（`cn.ts`、`format.ts`、`documentStatus.tsx`）

**后端服务**
- FastAPI 项目结构已建立：`backend/app/`
- 已实现接口：`/health`、`/api/v1/documents`、`/api/v1/providers`
- 数据库模型、CRUD、服务层、异步任务处理链路已建立

**Android 宿主**
- Capacitor Android 工程已初始化：`android/`
- 应用 ID：`com.karovia.kmap`
- 已引入核心插件：Filesystem、Share、App、FilePicker
- 仓库中已有 APK 构建产物（debug 和 release）

### 2.2 仍然只是原型或半成品的部分

**前端页面**
- `DashboardPage`：静态概览 UI，未接真实数据
- `GraphPage`：视觉化原型，未接真实图谱数据与交互
- `ChatPage`：静态聊天界面，未接真实聊天 API

**平台桥接**
- 平台接口已封装，但真实业务联调尚未完成
- Android 原生文件选择后的真实文件内容读取与上传闭环未完成
- 真机权限、分享、返回键、安全区体验未完整验证

**后端能力**
- 聊天接口未实现
- 图谱查询接口未实现
- 认证授权体系未实现
- 完整文档处理链路（解析、向量化、图谱构建）未完整联调

### 2.3 关键真实边界

- ✅ 已接入 React Router，不再使用 `currentTab` 单状态切页
- ✅ Capacitor 工程已生成，但不代表 Android 端已全部完成
- ✅ 平台接口已封装，但不代表原生能力已全部联调通过
- ⚠️ 页面存在 ≠ 业务已完成
- ⚠️ APK 已构建 ≠ 真机体验已验证

## 3. 当前模块状态判断口径

### 3.1 前端页面

| 页面 | 状态 | 说明 |
| --- | --- | --- |
| `DashboardPage` | 🟡 UI 原型 | 静态概览界面，未接真实数据 |
| `DocumentsPage` | ✅ 基础可用 | 前后端已联通，支持上传、列表、删除、状态轮询 |
| `GraphPage` | 🟡 UI 原型 | 视觉化原型，未接真实图谱数据与交互 |
| `ChatPage` | 🟡 UI 原型 | 静态聊天界面，未接真实聊天 API |
| `SettingsPage` | ✅ 基础可用 | 前后端已联通，支持服务商配置管理 |
| `ProviderGuidePage` | ✅ 已完成 | 静态说明页 |

### 3.2 后端接口

**已实现**
- `/health`：健康检查
- `/api/v1/documents`：文档上传、列表、删除、状态查询
- `/api/v1/providers`：服务商配置 CRUD、测试连接

**未实现**
- 聊天接口（对话历史、流式响应）
- 图谱查询接口（节点、关系、路径查询）
- 认证授权体系（用户、权限、会话）
- 完整文档处理链路（解析、向量化、图谱构建）

### 3.3 Android / 平台适配

**已实现**
- 平台识别（`platform/index.ts`）
- 文件选择接口封装（`pickDocument`）
- 权限检查/申请接口（`checkPermission`、`requestPermission`）
- 分享接口（`shareFile`）
- 保存文件接口（`saveFile`）
- 设备信息接口（`getDeviceInfo`）
- 返回键监听接口（`onBackButton`）

**未完成**
- 原生文件选择后的真实文件内容读取与上传闭环
- 真机联调验证（权限流程、分享体验、返回键处理）
- 安全区适配
- 软键盘适配
- 离线缓存与本地配置持久化

## 4. 当前代码架构与约定

### 4.1 路由与导航
- 路由方案：`React Router + HashRouter`
- 路由配置：在 `src/App.tsx` 中统一管理
- 页面切换动画：使用 `motion` 库在 `App.tsx` 中统一处理
- 底部导航：`src/components/navigation/BottomNav.tsx`

### 4.2 代码组织原则
- **页面层**：`src/pages/` 只负责页面级布局与状态编排，禁止直接散落 `fetch` 或平台调用
- **HTTP 请求**：统一走 `src/services/`（`httpClient.ts`、`documentsService.ts`、`providersService.ts`）
- **原生能力**：统一走 `src/platform/index.ts`
- **状态管理**：优先使用 Zustand store（`src/stores/`）或自定义 Hook（`src/hooks/`）
- **组件拆分**：`DocumentsPage` 与 `SettingsPage` 已完成首轮拆分，后续扩展优先补组件，不要重新写回单文件大页面

### 4.3 目录结构

```text
src/
├── App.tsx                    # 应用壳层、路由配置、页面切换动画
├── main.tsx                   # 入口文件、HashRouter
├── components/
│   ├── navigation/            # 导航组件
│   ├── documents/             # 文档相关组件
│   └── settings/              # 设置相关组件
├── data/                      # 模拟数据
├── hooks/                     # 自定义 Hook
├── pages/                     # 页面组件
├── platform/                  # 平台能力统一接口
├── services/                  # HTTP 服务层
├── stores/                    # Zustand 状态管理
├── types/                     # TypeScript 类型定义
└── utils/                     # 工具函数

backend/app/
├── main.py                    # FastAPI 入口
├── api/                       # API 路由
├── core/                      # 核心配置
├── crud/                      # 数据库 CRUD
├── db/                        # 数据库连接
├── models/                    # 数据库模型
├── schemas/                   # Pydantic 模型
├── services/                  # 业务逻辑层
├── utils/                     # 工具函数
└── worker/                    # 异步任务处理

android/                       # Capacitor Android 工程
```

## 5. 当前明显缺口与技术债

### 5.1 关键缺口

**P0 - 阻塞核心功能**
- Android 原生文件选择后的真实文件上传：`src/hooks/useDocuments.ts` 当前使用 `new File([], picked.name, ...)`，未从 URI 读取真实字节流
- 聊天接口与 ChatPage 接入：后端无聊天 API，前端无法完成对话功能
- 图谱接口与 GraphPage 接入：后端无图谱查询 API，前端无法展示知识图谱

**P1 - 影响开发体验**
- ESLint 配置不兼容：依赖已是 ESLint 9，但仓库仍使用 `.eslintrc.json`，需迁移到 `eslint.config.js` 或回退版本
- 工程命名未完成：根目录 `package.json` 的包名仍是 `react-example`

**P2 - 真机体验待验证**
- 权限流程真机验证
- 分享功能真机验证
- 返回键处理真机验证
- 安全区适配
- 软键盘适配

### 5.2 技术债

- 后端测试覆盖率低
- 前端缺少单元测试
- 文档处理链路未完整联调（解析 → 向量化 → 图谱构建）
- 认证授权体系未实现
- 离线缓存与本地配置持久化未实现

## 6. 协作原则

### 6.1 语言与文档
- 所有新增文档、注释、说明统一使用中文
- 代码注释使用中文
- Git commit message 使用中文

### 6.2 完成度描述规范
必须区分以下状态，不可混淆：
- ✅ **已落地**：功能完整，前后端已联通，可正常使用
- 🟡 **部分完成**：核心功能可用，但存在明显缺口或待优化项
- 🟡 **原型/骨架**：UI 已实现，但未接真实数据或业务逻辑
- ❌ **未实现**：尚未开始或仅有设计方案

### 6.3 代码修改原则
1. **先补类型，再补实现**
2. **优先做根因修复，不做表面性补丁**
3. **不在页面组件中直接写平台桥接调用**，统一经 `platform/` 出口
4. **不在页面组件中直接散落接口请求**，统一经 `services/` 出口
5. **页面扩展优先补组件**，不要重新写回单文件大页面
6. **保持最小化实现**，避免过度设计

### 6.4 时间判断
- 涉及时间判断时，使用明确日期，例如 `2026-03-11`
- 不使用模糊表述如"最近"、"刚刚"

## 7. 当前建议优先级

### P0 - 核心功能闭环

1. **补齐 Android 原生文件上传链路**
   - 从 URI 读取真实文件内容
   - 修复 `useDocuments.ts` 中的空 File 对象问题
   - 完成端到端文档上传验证

2. **完成聊天功能**
   - 后端实现聊天 API（对话历史、流式响应）
   - ChatPage 接入真实聊天接口
   - 实现对话上下文管理

3. **完成图谱功能**
   - 后端实现图谱查询 API（节点、关系、路径）
   - GraphPage 接入真实图谱数据
   - 实现图谱交互与可视化

### P1 - 开发体验与真机验证

4. **修复 ESLint 配置**
   - 迁移到 `eslint.config.js` 或回退 ESLint 版本
   - 恢复 `npm run lint` 可用性

5. **Android 真机联调**
   - 验证权限申请流程
   - 验证分享功能
   - 验证返回键处理
   - 适配安全区与软键盘

6. **完善文档处理链路**
   - 文档解析
   - 向量化
   - 图谱构建
   - 错误反馈优化

### P2 - 工程优化

7. **清理工程命名**
   - 修改 `package.json` 包名为 `kmap`
   - 清理无用依赖

8. **性能优化**
   - 图谱渲染性能评估
   - 大文件上传优化

9. **测试补充**
   - 后端单元测试
   - 前端组件测试

## 8. 验证信息（2026-03-11）

### 8.1 本次检查结果

- ✅ `npm run type-check`：通过
- ❌ `npm run lint`：失败，ESLint 9 与 `.eslintrc.json` 配置模式不兼容
- ⚠️ `vite build`：本次在受限 CLI 环境中触发 `spawn EPERM`，未能复验
- ⚠️ 后端测试：当前环境无可用 Python 运行时，未能复验

### 8.2 Git 状态

- 当前分支：`main`
- 主分支：`main`
- 最近提交：
  - `644ed7b` feat: 接入原生文件选择桥接能力
  - `806ef20` docs: 添加 GitHub Issue 推进计划文档
  - `36f186c` feat: 接入安卓首批桥接插件与平台能力

### 8.3 APK 构建产物

- Debug APK：`android/app/build/outputs/apk/debug/app-debug.apk`
- Release APK：`android/app/build/outputs/apk/release/app-release.apk`

注意：APK 存在不代表已完成真机验证

## 9. 项目当前阶段总结

Kmap 项目当前处于：**完成了基础 App 化与部分前后端联通，但尚未完成核心业务闭环**的阶段。

### 9.1 可用能力
- 文档管理：上传、列表、删除、状态查询（前后端已打通）
- 服务商配置：CRUD、测试连接（前后端已打通）
- Android 宿主：工程已初始化，APK 已构建

### 9.2 原型能力
- 首页概览（DashboardPage）
- 知识图谱（GraphPage）
- 智能问答（ChatPage）

### 9.3 待补齐能力
- Android 原生文件真实上传
- 聊天接口与前端接入
- 图谱接口与前端接入
- 认证授权体系
- 真机体验验证

---

**最后更新时间**：2026-03-11
