# Kmap 项目 Claude 配置文档

## 一、项目基本信息

- 项目名称：Kmap 智能知识图谱应用
- 产品交付形态：Android 本地应用
- 当前界面层基础：React 19 + TypeScript + Vite 6 + Tailwind CSS 4
- 当前界面层状态：已完成首轮模块化拆分，核心页面已迁移到 `src/pages/`
- 技术路线：使用现有 React 前端作为 App 内部界面层，通过 Android 宿主容器完成本地应用交付
- 后端形态：Python 3.11 + FastAPI + LangChain
- 文档与沟通语言：中文

## 二、架构决策

### 2.1 总体方案

- 项目只面向本地应用交付，不建设面向用户直接访问的 Web 产品
- 现有 React 前端继续作为 App 内部渲染层，而不是独立 Web 端
- Android 原生层负责文件选择、权限申请、文件系统、分享、下载、通知、返回键、启动参数等系统能力
- 优先保持一套界面层与业务逻辑，避免直接重写为 React Native 或原生 UI

### 2.2 当前实施进展

- `src/App.tsx` 已收敛为应用壳层
- 底部导航已拆到 `src/components/navigation/BottomNav.tsx`
- 5 个一级页面已拆到 `src/pages/`
- `src/services/`、`src/types/`、`src/platform/`、`src/utils/`、`src/data/` 已建立基础骨架

### 2.3 推荐技术选型

- Android 宿主：Capacitor Android
- 界面框架：React 19 + TypeScript + Vite
- 路由：React Router
- 状态管理：Zustand 或 React Context（优先轻量方案）
- API 层：统一 `services/` 封装，禁止在页面组件中散落 `fetch`
- 原生桥接：Capacitor Plugins，必要时补充自定义 Android Plugin

## 三、实施原则

1. 所有文档、注释、提交信息、PR 描述统一使用中文
2. 不把浏览器运行结果视为产品交付形态，浏览器仅用于开发调试
3. 继续沿着模块化方向重构，不在 `App.tsx` 中重新堆积业务代码
4. 业务逻辑、类型定义、接口封装必须与平台能力解耦
5. 涉及安卓能力的实现，必须先定义界面层接口，再补桥接适配层

## 四、前端开发规范

### 4.1 当前目录规范

- `src/pages/`：一级页面，如首页、文档、图谱、对话、设置
- `src/components/`：可复用组件
- `src/services/`：API 与平台能力调用封装
- 模型供应商配置分为 `chat` 与 `embedding` 两类，默认配置按用途分别生效
- `src/stores/`：后续全局状态目录
- `src/types/`：类型定义
- `src/hooks/`：后续通用 hooks 目录
- `src/platform/`：平台识别、原生桥接、能力适配
- `src/utils/`：通用工具函数
- `src/data/`：临时 mock 数据与静态展示数据

### 4.2 组件与代码规范

- 组件命名使用 PascalCase
- 变量与函数命名使用 camelCase
- 常量命名使用 UPPER_SNAKE_CASE
- 单个页面或组件超过 300 行时优先继续拆分
- 禁止在页面组件中直接写原生桥接调用，统一经 `platform/` 或 `services/` 出口
- 禁止滥用 `any`，确需使用时必须在同处说明原因

### 4.3 路由与状态规范

- 当前仍使用 `currentTab` 管理一级页面切换，下一阶段必须迁移到 React Router
- 页面状态、平台状态、会话状态分层管理，避免全部堆在根组件
- 异步状态统一包含 `loading`、`data`、`error`

### 4.4 API 与数据规范

- 所有接口请求统一收敛到 `src/services/`
- 统一处理超时、重试、鉴权、错误提示与响应转换
- 先定义类型，再实现接口调用
- `src/data/` 中的 mock 数据仅用于过渡，接入真实接口后及时移除

## 五、安卓 App 化规范

### 5.1 平台分层

- `ui-app`：页面 UI、交互、导航、状态、接口编排
- `platform-adapter`：平台检测、能力声明、桥接接口
- `android-shell`：Capacitor Android 容器与自定义原生插件

### 5.2 原生能力边界

- 必须原生化：文件选择、文件读写、权限申请、分享、通知、下载目录访问、返回键处理
- 优先由界面层实现：普通表单、仪表盘、列表页、聊天页、设置页
- 视性能决定：知识图谱渲染优先保留现有前端实现，若大图谱性能不足，再评估局部原生增强

### 5.3 桥接设计要求

- 界面层先定义能力接口，例如 `pickDocument()`、`saveFile()`、`requestStoragePermission()`
- 所有桥接返回统一结果对象，包含 `success`、`data`、`error`
- 调试环境下可提供浏览器 fallback，但不得反向驱动产品需求

## 六、阶段计划

### P0：App 化重构

- [x] 将 `src/App.tsx` 拆分为页面、组件、服务、类型、平台基础层
- [x] 建立 `src/pages/`、`src/components/`、`src/services/`、`src/types/`、`src/utils/`、`src/platform/`
- [ ] 引入 React Router
- [ ] 拆分文档页和设置页中的大块业务 UI
- [ ] 建立更明确的 `stores/` 和 `hooks/`

### P1：安卓宿主接入

- [ ] 引入 Capacitor Android 工程
- [ ] 完成基础容器启动、环境配置、打包流程
- [ ] 接入文件选择、权限、存储等首批桥接能力

### P2：移动端体验优化

- [ ] 优化底部导航、安全区、软键盘、返回键行为
- [ ] 适配移动端列表、图谱、聊天输入体验
- [ ] 增加离线缓存与启动性能优化

### P3：增强能力

- [ ] 下载与分享
- [ ] 推送通知
- [ ] 深链接与外部唤起
- [ ] 图谱性能专项优化

## 七、Git 工作流规范

1. 主分支：`main`
2. 功能分支：`feature/xxx`
3. 修复分支：`fix/xxx`
4. 文档分支：`docs/xxx`
5. 提交信息格式：`类型: 描述内容`
6. 一个提交聚焦一个明确目标，避免混杂重构、功能、样式与文档

## 八、技术决策记录

| 决策ID | 决策内容                                                   | 决策时间   | 负责人   |
| ------ | ---------------------------------------------------------- | ---------- | -------- |
| 1      | 产品仅交付 Android 本地应用，不建设面向用户的独立 Web 产品 | 2026-03-09 | 项目组   |
| 2      | 界面层保留 React + Vite 技术栈，作为 App 内部渲染层        | 2026-03-09 | 前端团队 |
| 3      | 安卓端采用 Capacitor Android 宿主承载界面层                | 2026-03-09 | 项目组   |
| 4      | 已完成首轮页面级拆分，后续继续推进路由和桥接接入           | 2026-03-09 | 前端团队 |
| 5      | 所有文档和沟通统一使用中文                                 | 2026-03-08 | 项目组   |

## 九、当前优先级

### 高优先级

- [ ] 引入 React Router
- [ ] 继续拆分 `DocumentsPage` 与 `SettingsPage`
- [ ] 明确安卓桥接能力清单与接口签名
- [ ] 接入 Capacitor Android

### 中优先级

- [ ] 完成文件上传与权限桥接
- [ ] 统一移动端交互规范
- [ ] 建立状态管理目录和基础 store

### 后续优先级

- [ ] 离线缓存
- [ ] 推送通知
- [ ] 图谱性能专项优化
