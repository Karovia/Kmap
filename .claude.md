# Kmap 项目 Claude 配置文档

## 一、项目基本信息
- 项目名称：Kmap 知识图谱应用
- 技术栈：前端 React 19 + TypeScript + Vite，后端待选型
- 代码语言：TypeScript 为主，少量 JavaScript

## 二、通用规范
1. 所有文档和代码注释使用中文编写
2. 与用户沟通、提交信息、PR描述全部使用中文
3. 代码必须符合 TypeScript 语法规范，禁止使用 any 类型（特殊情况需注明原因）

## 三、编码规范
### 前端规范
- 组件命名使用 PascalCase 风格，例如：`KnowledgeGraph.tsx`
- 函数和变量命名使用 camelCase 风格，例如：`getUserInfo`
- 样式类名使用 kebab-case 风格，例如：`graph-container`
- 每个组件单独一个文件，复杂度超过 500 行的组件需要拆分
- 所有 API 调用需要封装成独立的 service 函数，禁止在组件内直接写 fetch/axios 调用
- 类型定义统一放在 `src/types/` 目录下，按模块划分

### 后端规范
- **开发语言**：Python 3.11，遵循 PEP 8 编码规范
- **框架**：FastAPI 框架，接口命名遵循 RESTful 规范
- **接口规范**：
  - 所有接口必须返回统一的响应格式：`{"code": 200, "message": "success", "data": {}}`
  - 接口参数必须使用 Pydantic 做校验，返回明确的错误信息
  - 敏感接口必须添加权限校验
- **数据库规范**：
  - PostgreSQL 表名使用 snake_case 命名，字段名使用 snake_case
  - 数据库操作统一使用 SQLAlchemy ORM，禁止直接拼接SQL
  - 向量数据库Qdrant集合名称以 `collection_` 前缀开头
  - 图数据库Nebula Graph的空间名称以 `space_` 前缀开头
- **异步任务规范**：
  - 耗时任务（如文档解析、向量化）必须使用异步任务队列处理
  - 任务状态必须持久化存储，支持查询和进度更新
- **LLM集成规范**：
  - 所有LLM调用统一通过LangChain抽象层实现，禁止直接调用各厂商SDK
  - 自定义服务商支持必须兼容OpenAI接口格式
  - API调用必须添加超时、重试和限流机制

## 四、Git 工作流规范
1. 主分支：`main` 分支，用于发布生产环境
2. 开发分支：`dev` 分支，用于日常开发集成
3. 功能分支：`feature/xxx` 命名，用于开发新功能
4. Bug修复分支：`fix/xxx` 命名，用于修复bug
5. 提交信息格式：`类型: 描述内容`，类型包括：feat（新功能）、fix（修复）、docs（文档）、style（格式）、refactor（重构）、test（测试）、chore（构建/工具）
6. 每个提交必须对应一个明确的功能点或bug修复，禁止大而全的提交

## 五、代码审查规范
1. 所有代码合并到 dev/main 分支必须经过 PR 审查
2. PR 至少需要1人审查通过才能合并
3. 审查重点：代码正确性、性能、安全性、可读性、是否符合规范
4. 禁止合并存在明显bug或不符合规范的代码

## 六、技术决策记录
| 决策ID | 决策内容 | 决策时间 | 负责人 |
|--------|----------|----------|--------|
| 1 | 前端采用 React 19 + TypeScript + Vite + Tailwind CSS 技术栈 | 2026-03-08 | 前端团队 |
| 2 | 所有文档和沟通使用中文 | 2026-03-08 | 项目组 |

## 七、目录结构说明
```
Kmap/
├── src/              # 前端代码目录
│   ├── components/   # 公共组件
│   ├── pages/        # 页面组件
│   ├── services/     # API服务封装
│   ├── types/        # TypeScript类型定义
│   ├── utils/        # 工具函数
│   └── assets/       # 静态资源
├── docs/             # 项目文档目录
├── tests/            # 测试用例目录
└── README.md         # 项目说明文档
```
