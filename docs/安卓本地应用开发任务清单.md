# 安卓本地应用开发任务清单

**项目**：Kmap 安卓本地知识图谱应用
**目标平台**：Android
**当前阶段**：P0 向 P1 过渡
**开始日期**：2026-03-09

---

## 一、当前已完成事项

### 1. 界面层首轮模块化拆分

- [x] 将 `src/App.tsx` 收敛为应用壳层
- [x] 创建 `src/pages/` 并拆分 5 个一级页面
- [x] 创建 `src/components/navigation/BottomNav.tsx`
- [x] 创建 `src/services/api.ts`
- [x] 创建 `src/types/domain.ts` 与 `src/types/navigation.ts`
- [x] 创建 `src/utils/` 和 `src/platform/` 基础目录

### 2. 当前页面结构

- [x] `src/pages/DashboardPage.tsx`
- [x] `src/pages/DocumentsPage.tsx`
- [x] `src/pages/GraphPage.tsx`
- [x] `src/pages/ChatPage.tsx`
- [x] `src/pages/SettingsPage.tsx`

---

## 二、P0 剩余任务：继续完成 App 化重构

### 1. 路由改造

- [ ] 安装并配置 `react-router-dom`
- [ ] 将 `currentTab` 切换改为路由导航
- [ ] 建立页面路径与导航映射
- [ ] 处理默认路由和兜底路由

### 2. 页面继续拆分

#### 文档页

- [ ] 拆出上传进度组件
- [ ] 拆出文档列表项组件
- [ ] 拆出筛选栏组件
- [ ] 整理文档轮询逻辑为独立 hook

#### 设置页

- [ ] 拆出服务商列表组件
- [ ] 拆出服务商表单弹窗组件
- [ ] 拆出向量模型配置区块
- [ ] 整理服务商表单状态逻辑

### 3. 状态与平台分层

- [ ] 创建 `src/stores/`
- [ ] 为服务商配置建立基础 store
- [ ] 为文档列表建立基础 store 或 hook
- [ ] 为平台能力定义更完整的接口契约

### 4. 接口与数据整理

- [ ] 继续拆分 `src/services/api.ts` 为按领域划分的服务文件
- [ ] 清理仅用于展示的 mock 数据
- [ ] 统一接口错误处理格式

---

## 三、P1 任务：接入安卓宿主

### 1. Capacitor 基础接入

- [ ] 安装 `@capacitor/core`
- [ ] 安装 `@capacitor/cli`
- [ ] 初始化 `capacitor.config.ts`
- [ ] 执行 `npx cap add android`
- [ ] 建立 `android/` 工程

### 2. 构建产物接入

- [ ] 配置前端构建目录给 Capacitor 使用
- [ ] 执行 `npm run build`
- [ ] 执行 `npx cap sync android`
- [ ] 用 Android Studio 打开并运行

### 3. 首批桥接能力

- [ ] `pickDocument()`
- [ ] `requestStoragePermission()`
- [ ] `checkStoragePermission()`
- [ ] `shareContent()`
- [ ] `saveFile()`
- [ ] `handleBackButton()`

---

## 四、P2 任务：安卓体验优化

### 1. 移动端体验

- [ ] 适配安全区
- [ ] 适配软键盘顶起行为
- [ ] 优化底部导航点击体验
- [ ] 优化长列表滚动体验

### 2. 图谱与聊天体验

- [ ] 验证图谱页面在容器中的渲染性能
- [ ] 优化聊天输入区和发送操作
- [ ] 处理返回键与页面栈行为

### 3. 本地能力增强

- [ ] 接入本地配置持久化
- [ ] 设计离线缓存策略
- [ ] 评估通知能力接入

---

## 五、里程碑

- [x] 里程碑 1：完成首轮页面级拆分
- [ ] 里程碑 2：完成 React Router 接入
- [ ] 里程碑 3：完成 Capacitor Android 初始化
- [ ] 里程碑 4：打通文件选择与权限桥接
- [ ] 里程碑 5：生成首个可安装 APK

---

## 六、当前优先顺序

1. 引入 React Router
2. 继续拆分 `DocumentsPage` 与 `SettingsPage`
3. 接入 Capacitor Android 工程
4. 实现首批桥接能力
5. 真机验证与体验优化
